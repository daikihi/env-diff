# env-diff

This project shows the difference between two configuration files.
A project contains several configuration files for individual environments.
For example, a project may contain a configuration for a local environment and a production environment.
Sometimes, a developer wants to see the difference between the two environments.